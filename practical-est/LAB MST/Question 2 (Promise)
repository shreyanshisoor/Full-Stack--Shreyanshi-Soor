const dataPromise = new Promise((resolve, reject) => {
    setTimeout(() => {
        const isSuccess = true;
        if (isSuccess) {
            resolve("Data fetched successfully");
        } else {
            reject("Error: Failed to fetch data");
        }
    }, 2000);
});

//Even UID- Questions 2 and 4 have been submitted.
